"use client";
import PropertyDetail from "../../Common/PropertyDetail/PropertyDetail";

const PropertyImageSliderContainer = () => {
  return <PropertyDetail type='iamge-slider' />;
};

export default PropertyImageSliderContainer;
