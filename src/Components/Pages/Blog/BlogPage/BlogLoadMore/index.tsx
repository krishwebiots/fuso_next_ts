"use client";
import BlogGridView from "@/Components/CommonComponents/BlogGridView";

const BlogLoadMoreContainer = () => {
  return <BlogGridView gridSize={3} type='load-more' />;
};

export default BlogLoadMoreContainer;
